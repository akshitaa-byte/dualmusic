import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/Providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Stereo Split Music Player",
  description: "Play two tracks simultaneously in left and right ears with native Web Audio API",
};

/**
 * Root Application Layout.
 * 
 * WHAT: Wraps the entire Next.js application tree with global styles, fonts, and authentication session providers.
 * WHY: Enables global auth state availability (`SessionProvider`) and consistent baseline typography across all pages.
 */
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
